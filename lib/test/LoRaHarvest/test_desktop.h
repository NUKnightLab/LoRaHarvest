/**
 * Desktop-only tests
 * This module is provided for completeness. It seems unlikely that any desktop-only tests
 * would be needed for an embedded project. If there are no desktop-only tests, this file and
 * test_desktop.cpp be deleted along with any relevant `test_ignore` references in platform.ini.
 */

#ifndef TEST_DESKTOP_H
#define TEST_DESKTOP_H

namespace Test_DesktopThings {
    void test_all();
}

#endif